test.server=c2cpc83.camptocamp.com
#test.server=tc-geocat.bgdi.admin.ch
locale=eng
# options include basic and default
# See Login object
login=basic
# Options are the class name of the SystemLifeCycle to use.
# If the class is in the org.fao.geonet package the package can be omitted
# Also see docs of the implementation to know what additional properties are required
# See SystemLifeCycle implementations
lifecycle=PredefinedUserLifeCycle
user=jeichar
pass=jeichar
